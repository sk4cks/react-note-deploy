apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: note-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/sk4cks/react-note-deploy.git
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: note
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
